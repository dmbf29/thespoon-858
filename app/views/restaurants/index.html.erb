<div class="container">
  <h1>Restaurants</h1>
  <% @restaurants.each do |restaurant| %>
    <p class="m-0"><%= link_to restaurant.name, restaurant_path(restaurant), class: 'btn-light' %></p>
    <small><%= restaurant.address %></small>
  <% end %>
</div>
